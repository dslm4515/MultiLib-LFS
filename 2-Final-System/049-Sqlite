# Final System - Sqlite 
# Build and install under chroot

# Prepare sqlite for 64-bit compilation: 
export  CARGS="--prefix=/usr "
export CARGS+="--disable-static "
export CARGS+="--enable-fts4 "
export CARGS+="--enable-fts5 "

export  XFLGS="-D SQLITE_ENABLE_COLUMN_METADATA=1 "
export XFLGS+="-D SQLITE_ENABLE_UNLOCK_NOTIFY=1 "
export XFLGS+="-D SQLITE_ENABLE_DBSTAT_VTAB=1 "
export XFLGS+="-D SQLITE_SECURE_DELETE=1 "

./configure $CARGS CPPFLAGS="${XFLGS}"

# Compile the package:
make LDFLAGS.rpath=""

# Install the package: 
make install

## 32-bit Sqlite

# Clean the previous build
make distclean

# Prepare sqlite for 32-bit compilation:
export CARGS+="--host=i686-pc-linux-gnu "
export CARGS+="--libdir=/usr/lib32 "
CC="gcc -m32" CXX="g++ -m32" ./configure $CARGS CPPFLAGS="${XFLGS}"
unset CARGS XFLGS

# Compile the package:
make LDFLAGS.rpath=""

# Install the package: 
make DESTDIR=$PWD/DESTDIR install
cp -Rv DESTDIR/usr/lib32/* /usr/lib32
rm -rf DESTDIR

