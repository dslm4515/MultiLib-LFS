# Final System - Gzip
# Build and install under chroot

# Prepare gzip for 64-bit compilation: 
./configure --prefix=/usr

# Compile the package:
make 

# To test the results, issue: 
make check

# Install the package: 
make install

